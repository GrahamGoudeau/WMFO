SELECT
    id,
    first_name,
    last_name,
    email,
    active,
    tufts_id,
    last_agreement_signed,
    date_last_agreement_signed,
    permission_levels,
    confirmed_volunteer_hours,
    pending_volunteer_hours,
    num_shows_hosted
FROM all_user_info_v;
